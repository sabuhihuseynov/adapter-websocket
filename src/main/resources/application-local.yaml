server:
  port: 9000

logging:
  level:
    ROOT: info
    com.guavapay.dbs: DEBUG
    org.hibernate: DEBUG

spring:
  application:
    name: adapter-websocket
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest